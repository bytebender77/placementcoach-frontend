{
  "name": "placementcoach-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "echo 'Static site — no build step needed'",
    "preview": "npx serve . -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
